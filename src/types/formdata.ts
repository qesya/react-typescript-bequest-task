type FormData = {
    postcode: string;
    name: string;
    phone: string;
    email: string;
    town: string;
    country: string;
    first_address: string;
    second_address: string;
};

export type {
    FormData,
}